import {ExamplesIndex} from '../components';

<ExamplesIndex getThumbnail={item => `/images/examples/${item.docId || item.label.match(/[A-Z]{2,}(?=[A-Z][a-z]+[0-9]*|\b)|[A-Z]?[a-z]+[0-9]*|[A-Z]|[0-9]+/g).join('-').toLowerCase()}.jpg`} />
